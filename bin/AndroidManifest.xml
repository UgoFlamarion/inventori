<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.ugo.inventori"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="17"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light" >
        <activity
            android:name="fr.ugo.inventori.activity.ItemListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="fr.ugo.inventori.activity.OrderListActivity"
            android:label="@string/order_list_activity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity -->
        <!-- android:name="fr.ugo.inventori.activity.ItemDetailActivity" -->
        <!-- android:label="@string/title_item_detail" -->
        <!-- android:parentActivityName=".activity.ItemListActivity" > -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".ItemListActivity" /> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name="fr.ugo.inventori.activity.ItemNewActivity" -->
        <!-- android:label="@string/title_item_new" -->
        <!-- android:parentActivityName=".activity.ItemNewActivity" > -->
        <!-- <meta-data -->
        <!-- android:name="android.support.PARENT_ACTIVITY" -->
        <!-- android:value=".ItemNewActivity" /> -->
        <!-- </activity> -->
    </application>

</manifest>